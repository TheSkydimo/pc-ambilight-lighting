---
import { DEFAULT_LOCALE } from "~/i18n/config";
import BaseLayout from "~/layouts/BaseLayout.astro";
import { localizePathname } from "~/i18n/config";
import { buildPageData } from "~/i18n/page-data";

const locale = DEFAULT_LOCALE;
const { site } = Astro;
const pageData = buildPageData(locale, site, "/");
const { copy } = pageData;
---

<BaseLayout locale={locale} pathname="/">
  <section class="grid gap-10 lg:grid-cols-[minmax(0,1fr)_22rem]">
    <div class="space-y-6">
      <p class="text-sm uppercase tracking-wide text-offset">
        PC · Ambilight · Screen Sync RGB
      </p>
      <h1 class="text-balance text-4xl font-semibold tracking-tight sm:text-5xl">
        {copy.home.h1}
      </h1>
      <p class="max-w-2xl text-pretty text-lg text-offset">
        {copy.home.valueProp}
      </p>
      <div class="flex flex-wrap gap-3">
        <a class="btn rounded border border-default px-4 py-2 text-sm" href={localizePathname(locale, "/best-screen-sync-rgb-software/")}>
          Start: low-friction picks
        </a>
        <a class="btn rounded border border-default px-4 py-2 text-sm" href={localizePathname(locale, "/guides/")}>
          Setup in 5 minutes
        </a>
      </div>
    </div>
    <aside class="rounded-2xl border border-default bg-offset p-6">
      <div class="text-sm font-semibold">If you are new, use this order</div>
      <ul class="mt-4 space-y-2 text-sm text-offset">
        <li class="flex gap-2">
          <span aria-hidden="true" class="text-secondary">•</span>
          <span>Pick a stable setup first from the recommendations</span>
        </li>
        <li class="flex gap-2">
          <span aria-hidden="true" class="text-secondary">•</span>
          <span>Follow the setup guide, then tune latency</span>
        </li>
        <li class="flex gap-2">
          <span aria-hidden="true" class="text-secondary">•</span>
          <span>If detection fails, use compatibility troubleshooting</span>
        </li>
      </ul>
    </aside>
  </section>

  <section class="mt-16">
    <header class="space-y-2">
      <h2 class="text-2xl font-semibold tracking-tight">Start here (3 steps)</h2>
      <p class="max-w-3xl text-sm text-offset">
        The homepage only gives your starting path. Deep comparisons and long FAQs stay on inner pages.
      </p>
    </header>
    <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <a class="group rounded-2xl border border-default bg-offset p-5 transition hover:border-secondary" href={localizePathname(locale, "/best-ambilight-software/")}>
        <div class="text-sm font-semibold">1) Choose a setup</div>
        <div class="mt-2 text-sm text-offset">Use curated picks to decide quickly with fewer trade-offs.</div>
        <div class="mt-4 text-xs text-offset transition-colors group-hover:text-secondary">{copy.common.readMore} →</div>
      </a>
      <a class="group rounded-2xl border border-default bg-offset p-5 transition hover:border-secondary" href={localizePathname(locale, "/guides/pc-ambilight-setup-tutorial/")}>
        <div class="text-sm font-semibold">2) Install quickly</div>
        <div class="mt-2 text-sm text-offset">Follow the practical setup flow before fine-tuning.</div>
        <div class="mt-4 text-xs text-offset transition-colors group-hover:text-secondary">{copy.common.readMore} →</div>
      </a>
      <a class="group rounded-2xl border border-default bg-offset p-5 transition hover:border-secondary" href={localizePathname(locale, "/compatibility/not-detected/")}>
        <div class="text-sm font-semibold">3) Troubleshoot fast</div>
        <div class="mt-2 text-sm text-offset">Use the decision tree when devices are not detected.</div>
        <div class="mt-4 text-xs text-offset transition-colors group-hover:text-secondary">{copy.common.readMore} →</div>
      </a>
    </div>
  </section>

  <section class="mt-16">
    <header class="space-y-2">
      <h2 class="text-2xl font-semibold tracking-tight">Common entry points</h2>
      <p class="max-w-3xl text-sm text-offset">
        You do not need to read everything on the homepage. Jump to the page that matches your current goal.
      </p>
    </header>
    <div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <a class="rounded-2xl border border-default bg-offset p-5 hover:border-secondary" href={localizePathname(locale, "/reviews/")}>
        <div class="text-sm font-semibold">{copy.nav.reviews}</div>
        <div class="mt-2 text-sm text-offset">Read in depth when you already have shortlist candidates.</div>
      </a>
      <a class="rounded-2xl border border-default bg-offset p-5 hover:border-secondary" href={localizePathname(locale, "/comparisons/")}>
        <div class="text-sm font-semibold">{copy.nav.comparisons}</div>
        <div class="mt-2 text-sm text-offset">Use A-vs-B pages to validate a near-final decision.</div>
      </a>
      <a class="rounded-2xl border border-default bg-offset p-5 hover:border-secondary" href={localizePathname(locale, "/guides/")}>
        <div class="text-sm font-semibold">{copy.nav.guides}</div>
        <div class="mt-2 text-sm text-offset">Practical setup and tuning workflows.</div>
      </a>
    </div>
  </section>
</BaseLayout>
